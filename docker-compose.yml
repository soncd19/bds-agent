services:
  bds-agent:
    build:
      context: .
    image: bds-agent:latest
    container_name: bds-agent
    restart: unless-stopped
    environment:
      PORT: "5173"
      NODE_ENV: production
    ports:
      - "5173:5173"
    volumes:
      - ./data:/app/data
